# bilibili 弹幕屏蔽规则清单

- `(阿巴)+`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `阿巴阿巴`
  - 排除示例：
    无
- `(草|操|🌿|艹)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `我操`
    - `太生草了`
    - `🌿`
    - `艹`
  - 排除示例：
    无
- `(顿|炖|遁|盾|挪)(出|煮)红(尘|城)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `顿出红尘`
    - `炖煮红尘`
    - `遁出红尘`
    - `盾出红尘`
    - `挪出红尘`
    - `炖出红城`
  - 排除示例：
    无
- `(鸡|姬)(你|霓)太美`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `鸡你太美`
    - `姬霓太美`
  - 排除示例：
    无
- `(没|难)?(蚌|崩|绷)(埠|不)?(住)?(了)?`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `蚌`
    - `蚌埠住了`
    - `崩不住了`
    - `没绷住`
    - `难蚌`
  - 排除示例：
    无
- `(爷|奶)(青|童)(结|回)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `爷青结`
    - `奶青回`
    - `爷童回`
  - 排除示例：
    无
- `(一|亿)拍(即|既)合`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `一拍即合`
    - `亿拍即合`
    - `亿拍既合`
  - 排除示例：
    无
- `((八|巴|吧)个?(人|仁)|迈)(八|巴|拔)?个?(鹤|鶴|赫|河|盒|核)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `八人八鹤`
    - `八人巴赫`
    - `八人拔河`
    - `八人八鶴`
    - `八人八盒`
    - `八人八核`
    - `巴人八鹤`
    - `迈巴鹤经销商`
    - `八人八个鹤`
    - `八仁八鹤`
    - `吧人巴赫`
    - `八人鹤`
  - 排除示例：
    无
- `((唯|惟|但)手熟|自动挡?)尔`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `唯手熟尔`
    - `惟手熟尔`
    - `无它，但手熟尔`
    - `无他自动挡尔`
    - `无他，自动尔`
  - 排除示例：
    无
- `(?<!乾)坤`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `一坤年`
    - `一百坤`
  - 排除示例：
    - `林间松韵，石上泉声，静里听来，识天地自然鸣佩；草际烟光，水心云影，闲中观去，见乾坤最上文章`
- `.+播放.+赞`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `一播放13赞`
  - 排除示例：
    无
- `.+の.+`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `萨隆の关心`
    - `智慧の眼神`
  - 排除示例：
    无
- `[Ss][Hh][Ii][Tt]`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `SHIT`
    - `shit`
  - 排除示例：
    无
- `^啊对{3,}$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `啊对对对`
  - 排除示例：
    无
- `^出院`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `^好耶`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `^桀{3,}$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `桀桀桀`
  - 排除示例：
    无
- `^开播大吉`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `^泪(目|了)$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `泪目`
    - `泪了`
  - 排除示例：
    无
- `^路走(窄|宽)了$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `路走窄了`
    - `路走宽了`
  - 排除示例：
    无
- `^完结撒花`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `^唯.真(神|史)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `唯一真神`
    - `唯一真史`
    - `唯二真神`
  - 排除示例：
    无
- `^危[\s.。]*`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `危..`
  - 排除示例：
    无
- `^(爱了)+`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `爱了`
    - `爱了爱了`
  - 排除示例：
    无
- `^(多|夺)少[?？]?`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `多少`
    - `夺少?`
  - 排除示例：
    无
- `^(盖|迪).*(亚|迦).*$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `盖亚`
    - `盖~亚~`
    - `盖亚！！！！`
    - `迪迦！！！！`
  - 排除示例：
    无
- `^(妙|秒)啊$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `妙啊`
    - `秒啊`
  - 排除示例：
    无
- `^(天才|甜菜)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `天才`
    - `甜菜`
  - 排除示例：
    无
- `^(我|哇|蛙)(去|趣)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `我去`
    - `哇趣`
    - `蛙趣`
  - 排除示例：
    无
- `^.*(丸|完)辣$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `丸辣`
    - `完辣`
  - 排除示例：
    无
- `^.+狂喜$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `双重狂喜`
    - `三厨狂喜`
    - `师傅狂喜`
  - 排除示例：
    无
- `^.+(万岁|萬歲)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `人民万岁`
    - `人民萬歲！！！`
  - 排除示例：
    无
- `^[Aa][Ww][Ss][Ll]$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `AWSL`
    - `awsl`
  - 排除示例：
    无
- `^[Oo][Pp]$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `op`
  - 排除示例：
    无
- `^[\d\.\*\+\-\/\|。\?？\!！,，~～;；、…哈阿啊嗨哇哦嗯嘿耶呜哒啦拉嗷\\]+$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `哈哈哈`
    - `啊啊啊`
    - `嗨嗨嗨`
    - `呜呜呜`
    - `???`
    - `。。。`
    - `～～～`
    - `|||`
    - `;;;`
    - `啊?`
    - `嗯?`
    - `哇哦`
    - `耶！`
    - `*****-+`
    - `.0.0.0..0`
    - `………………6`
    - `哒，哒哒哒哒～`
    - `嗷嗷嗷，`
  - 排除示例：
    无
- `^[^\d]+[:：][^\d]+$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `月亮：神金`
    - `天美：我不知道啊`
    - `灰太狼：6`
  - 排除示例：
    - `3:54`
- `^\d+$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `111`
    - `2233`
    - `66666`
    - `99999`
  - 排除示例：
    无
- `^h{3,}$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `hhh`
    - `hhhhhh`
  - 排除示例：
    无
- `^《.+》$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `《相貌平平》`
    - `《氨醛》`
  - 排除示例：
    无
- `奥(力|利|里)(给|怪)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `奥里给`
    - `奥力给`
    - `奥利给`
    - `奥利怪`
  - 排除示例：
    无
- `fw`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `感谢大自然的馈赠`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `鸽鸽`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `沟槽`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `好家伙`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    - `好家伙，我直接八集连播！`
  - 排除示例：
    无
- `火钳刘明`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `今夕是何年`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `恐怖如斯`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `梦开(始|启)的(地方|时候)`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `梦开始的地方`
    - `梦开始的时候`
    - `梦开启的地方`
  - 排除示例：
    无
- `命运的齿轮开始转动`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `念头不?通达`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `念头通达`
    - `念头不通达了`
  - 排除示例：
    无
- `全体起立`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `神经，害我笑了一下`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    - `神经，害我笑了一下`
  - 排除示例：
    无
- `塔塔开`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `我(了|嘞)个豆`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `我了个豆`
    - `我嘞个豆`
    - `好超前，我了个豆`
  - 排除示例：
    无
- `下次也?(一|丕)定`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `下次一定`
    - `下次丕定`
    - `下次也不一定`
  - 排除示例：
    无
- `仙之人兮列如麻`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `永不为奴$`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `LCD 永不为奴`
    - `580 永不为奴`
  - 排除示例：
    无
- `真神登场`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `只因`
  - 类型：文本
  - 是否启用：是
  - 匹配示例：
    无
  - 排除示例：
    无
- `助力每.*一个.*梦想`
  - 类型：正则
  - 是否启用：是
  - 匹配示例：
    - `助力每一个梦想`
    - `助力每一个不知死活的梦想`
    - `助力每日一个梦想`
  - 排除示例：
    无
